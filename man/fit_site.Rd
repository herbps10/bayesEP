% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_site.r
\name{fit_site}
\alias{fit_site}
\title{Fit a single site's tilted distribution and compute EP site update}
\usage{
fit_site(site_data, d, Q_global, r_global, Q_site, r_site, eta = 1, fit_model)
}
\arguments{
\item{site_data}{Data frame containing the observations for this site.}

\item{d}{Integer. Dimension of the shared parameter vector \eqn{\phi}.}

\item{Q_global}{Numeric \eqn{d \times d} matrix. Current global precision
matrix.}

\item{r_global}{Numeric vector of length \eqn{d}. Current global precision
mean.}

\item{Q_site}{Numeric \eqn{d \times d} matrix. Current site precision matrix.}

\item{r_site}{Numeric vector of length \eqn{d}. Current site precision mean.}

\item{eta}{Numeric in \eqn{(0, 1]}. Power EP parameter. \eqn{\eta = 1} (default)
corresponds to standard EP with KL divergence minimization. Values less
than one correspond to power EP with \eqn{\alpha}-divergence.}
}
\value{
A named list with the following elements:
\describe{
\item{delta_Q}{Numeric \eqn{d \times d} matrix. Change in site precision
matrix. Zero matrix if site failed or was skipped.}
\item{delta_r}{Numeric vector of length \eqn{d}. Change in site precision
mean. Zero vector if the site failed or was skipped.}
\item{status}{Character string: \code{"success"}, \code{"skipped"} (improper cavity
distribution), or \code{"failed"} (\code{fit_model} returned \code{NULL}).}
\item{fit}{The full return value from \code{fit_model}, or \code{NULL} if skipped/failed.}
\item{phi_draws}{Numeric matrix of shared parameter draws from the tilted
distribution (\eqn{n} rows \eqn{\times d} columns), or \code{NULL} if
skipped/failed.}
}
}
\description{
Internal function called by \code{\link[=fit_ep]{fit_ep()}} for each site at each EP iteration.
Computes the cavity distribution by removing the current site's contribution
from the global approximation, fits the tilted distribution using the
user-supplied \code{fit_model} function, and returns the change in site natural
parameters needed to update the global approximation.
}
\seealso{
\code{\link[=fit_ep]{fit_ep()}} which calls this function iteratively for each site.
}
\keyword{internal}
